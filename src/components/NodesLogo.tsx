import { XStack } from 'tamagui'

const NodesLogo = () => {
  return (
    <XStack
      style={{
        alignItems: 'center',
      }}
      space={'$2'}
    ></XStack>
  )
}

export default NodesLogo
